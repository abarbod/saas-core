import { AppShell } from '../../../components/app-shell';

export default function SettingsPage() {
  return (
    <AppShell title="Settings">
      <section className="card">
        <h3>Tenant switcher</h3>
        <p>UI placeholder for multi-tenant context switching.</p>
      </section>
    </AppShell>
  );
}

export default function LoginPage() {
  return (
    <main className="layout card">
      <h2>Sign in</h2>
      <p>Tenant-aware JWT auth flow scaffold. Wire to /api/v1/auth/login.</p>
    </main>
  );
}
